---
"$id": http://ga4gh.org/schemas/discovery/search/components/collection/count
"$schema": http://json-schema.org/draft-07/schema#
description: |
  This component represents the number of record that were found for a given search request from a server.
type: object
required:
  - count
properties:
  count:
    type: number
    examples:
      - 0
      - 1
      - 1000